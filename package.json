{
  "name": "minisim-scicicd",
  "version": "0.1.0",
  "homepage": "https://pmocz.github.io/minisim-scicicd/",
  "description": "A scicicd GitHub Page for the minisim project",
  "license": "MIT",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "predeploy": "rm -rf dist && vite build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "babel-preset-nano-react-app": "^0.1.0",
    "gh-pages": "^6.1.1",
    "vite": "^5.2.6"
  },
  "babel": {
    "presets": [
      "nano-react-app"
    ]
  }
}
